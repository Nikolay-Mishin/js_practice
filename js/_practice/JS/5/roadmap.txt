1) Скачать данные
http://mxn42.ru/avalon/data/planets.json

2) Подключить к странице
    2.1  index.html
    2.2  planets.json --> planets.js
         добавить переменную в файл
         planets = ...
    2.3  в index.html поключить planets.js

3) Вывести на страницу количество планет
    3.1  прочитать значение data и найти длину массива
    3.2  добавить на страницу место для вывода
    3.3 вывести количество в 3.2

4) Вывести на страницу таблицу с колонками
   Название планеты | Удаленность от солнца
   p.name             pl['mean distance from Sun (AU)']

   <table>
    <tbody></tbody>
   </table>

   let tbody = document.querySelector('table > tbody')
   tbody.innerHTML = ''
   for (let planet of planets.data) {
        tbody.innerHTML += '<tr><td> ...
   }

5) Код приложения выделить в отдельный файл
6) Определить функцию main и запускать ее когда страница готова (DOMContentLoaded).
7) Создание таблицы вынести в отдельную функцию

 - подключение json, CORS
 - сортировка
 - На степик!
    - работа с функциями
    - методы document
    - тернарный оператор
    - JSON
 - фильтрацию по группа
 - добавление / редактирования / удаление
 - работа с датами
 - подключение стилей
