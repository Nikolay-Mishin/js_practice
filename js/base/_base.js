/* ...................................... 

    	BASE
		
...................................... */

string_split ('bbb', "", '', 'A');
string_replace ('somestring\\w', '\\w', '@');

// Задание 1

click_file (1);

// Задание 2

var a = 1, b = 2, c = 3;
formula_1 (2, a , b ,c);

// Задание 7

var old_value = "";
var cur_value = document.getElementById ('a_7');

if (watch_changes (old_value, cur_value)) {
	
}

input_validate (7, a_7, output_7);
input_symbol_ie (a_7, output_symbol_ie, "короткая", 6);

split_separate ("a b c", " ");
